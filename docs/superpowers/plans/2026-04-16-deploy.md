# Deploy M.me — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Portare il progetto M.me in produzione su VPS Hostinger con Nginx, PM2, PostgreSQL e SSL.

**Architecture:** Nginx serve la SPA React da `/opt/M.me/frontend/dist` e fa proxy di `/api/*` e `/uploads/*` a Fastify (porta 3001) gestito da PM2. Un unico dominio `mirigliani.me`.

**Tech Stack:** Fastify v5, Prisma v7, PostgreSQL 16, React/Vite, Nginx 1.24, PM2 6, Certbot, Node 22

> **ISOLAMENTO CRITICO:** Questo deploy tocca SOLO `/opt/M.me`, PM2 `mme`, DB `"M.me"`, Nginx `mirigliani.me`.
> Non toccare: `mps`, `bhs`, `mirigliani.cloud`, `/opt/mps/`.

---

## File Map

| File | Azione | Scopo |
|---|---|---|
| `ecosystem.config.js` | Crea | Config PM2 per il processo `mme` |
| `deploy.sh` | Crea | Script deploy ripetibile (git pull + build + migrate + restart) |
| `backend/.env.example` | Crea | Template variabili d'ambiente senza segreti |
| `docs/nginx/mirigliani.me` | Crea | Config Nginx completa da copiare sul VPS |
| VPS `/opt/M.me/backend/.env` | Crea (VPS) | Variabili reali con segreti — mai committato |
| VPS `/etc/nginx/sites-available/mirigliani.me` | Modifica (VPS) | Sostituisce config esistente (solo HTTP) |

---

## Task 1: ecosystem.config.js

**Files:**
- Create: `ecosystem.config.js`

- [ ] **Step 1: Crea il file**

```js
module.exports = {
  apps: [{
    name: 'mme',
    script: './backend/dist/app.js',
    cwd: '/opt/M.me',
    instances: 1,
    node_args: '--max-old-space-size=512',
    env: { NODE_ENV: 'production' },
    error_file: '/var/log/pm2/mme-error.log',
    out_file: '/var/log/pm2/mme-out.log',
  }]
}
```

- [ ] **Step 2: Verifica sintassi**

```bash
node -e "require('./ecosystem.config.js'); console.log('OK')"
```
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add ecosystem.config.js
git commit -m "feat(deploy): ecosystem.config.js PM2 per processo mme"
```

---

## Task 2: deploy.sh

**Files:**
- Create: `deploy.sh`

- [ ] **Step 1: Crea il file**

```bash
#!/bin/bash
set -e

# ISOLAMENTO: tocca SOLO /opt/M.me e PM2 "mme"
# NON modificare: mps, bhs, mirigliani.cloud

cd /opt/M.me
git pull origin main

cd backend
npm ci
npm run build
npx prisma migrate deploy

cd ../frontend
npm ci
npm run build

pm2 restart mme
echo "Deploy M.me completato."
```

- [ ] **Step 2: Rendi eseguibile**

```bash
chmod +x deploy.sh
```

- [ ] **Step 3: Commit**

```bash
git add deploy.sh
git commit -m "feat(deploy): script deploy ripetibile con isolamento MPS"
```

---

## Task 3: backend/.env.example

**Files:**
- Create: `backend/.env.example`

- [ ] **Step 1: Crea il file**

```env
# Database
DATABASE_URL="postgresql://mirigliani_usr:PASSWORD@localhost:5432/M.me"

# Session (genera con: openssl rand -hex 32)
SESSION_SECRET=sostituisci_con_64_char_hex
SESSION_SALT=sostituisci_con_32_char_hex

# App
NODE_ENV=production
PORT=3001
```

- [ ] **Step 2: Verifica che .env NON sia nel repo**

```bash
cat backend/.gitignore | grep .env
```
Expected: `.env` presente nel gitignore.

Se mancante:
```bash
echo ".env" >> backend/.gitignore
```

- [ ] **Step 3: Commit**

```bash
git add backend/.env.example backend/.gitignore
git commit -m "feat(deploy): .env.example template variabili produzione"
```

---

## Task 4: Config Nginx

**Files:**
- Create: `docs/nginx/mirigliani.me`

- [ ] **Step 1: Crea directory**

```bash
mkdir -p docs/nginx
```

- [ ] **Step 2: Crea il file config Nginx**

```nginx
# /etc/nginx/sites-available/mirigliani.me
# Nginx per M.me — SPA + proxy /api/ + /uploads/ → Fastify 3001
# NON toccare: mirigliani.cloud

server {
    listen 80;
    server_name mirigliani.me www.mirigliani.me;
    return 301 https://mirigliani.me$request_uri;
}

server {
    listen 443 ssl;
    server_name www.mirigliani.me;
    ssl_certificate     /etc/letsencrypt/live/mirigliani.me/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/mirigliani.me/privkey.pem;
    return 301 https://mirigliani.me$request_uri;
}

server {
    listen 443 ssl;
    server_name mirigliani.me;
    ssl_certificate     /etc/letsencrypt/live/mirigliani.me/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/mirigliani.me/privkey.pem;

    root /opt/M.me/frontend/dist;
    index index.html;

    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        client_max_body_size 350m;
    }

    location /uploads/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

- [ ] **Step 3: Commit**

```bash
git add docs/nginx/mirigliani.me
git commit -m "feat(deploy): config Nginx mirigliani.me con proxy API e uploads"
```

- [ ] **Step 4: Push su GitHub**

```bash
git push origin main
```

---

## Task 5: VPS — Database e utente PostgreSQL

**Connessione:** `ssh bhs-vps` (alias configurato in ~/.ssh/config)

- [ ] **Step 1: Crea utente e database**

```bash
ssh bhs-vps
sudo -u postgres psql -c "CREATE USER mirigliani_usr WITH PASSWORD 'SCEGLI_PASSWORD_SICURA';"
sudo -u postgres psql -c "CREATE DATABASE \"M.me\" OWNER mirigliani_usr;"
```

- [ ] **Step 2: Verifica**

```bash
sudo -u postgres psql -c "\l" | grep "M.me"
```
Expected: riga con `M.me | mirigliani_usr`

- [ ] **Step 3: Testa connessione**

```bash
psql "postgresql://mirigliani_usr:SCEGLI_PASSWORD_SICURA@localhost:5432/M.me" -c "SELECT 1;"
```
Expected: `1` — connessione OK.

---

## Task 6: VPS — Git setup e clone repo

**Nota:** `/opt/M.me/` esiste già con directory vuote `backend/`, `frontend/`, `uploads/`.

- [ ] **Step 1: Init git e collega remote**

```bash
cd /opt/M.me
git init
git remote add origin https://github.com/spawn5m/M.me
```

- [ ] **Step 2: Pull da main**

```bash
git pull origin main
```
Expected: tutti i file del repo scaricati.

- [ ] **Step 3: Verifica struttura**

```bash
ls backend/src/ && ls frontend/src/ && ls ecosystem.config.js deploy.sh
```
Expected: file presenti.

---

## Task 7: VPS — Crea .env

- [ ] **Step 1: Genera segreti**

```bash
SESSION_SECRET=$(openssl rand -hex 32)
SESSION_SALT=$(openssl rand -hex 16)
echo "SECRET: $SESSION_SECRET"
echo "SALT:   $SESSION_SALT"
```
Annota i valori — servono nel prossimo step.

- [ ] **Step 2: Crea .env da template**

```bash
cp /opt/M.me/backend/.env.example /opt/M.me/backend/.env
nano /opt/M.me/backend/.env
```

Compila con i valori reali:
```env
DATABASE_URL="postgresql://mirigliani_usr:SCEGLI_PASSWORD_SICURA@localhost:5432/M.me"
SESSION_SECRET=<valore da Step 1>
SESSION_SALT=<valore da Step 1>
NODE_ENV=production
PORT=3001
```

- [ ] **Step 3: Verifica (senza esporre segreti)**

```bash
grep "^NODE_ENV\|^PORT\|^DATABASE_URL" /opt/M.me/backend/.env
```
Expected:
```
DATABASE_URL="postgresql://mirigliani_usr:...@localhost:5432/M.me"
NODE_ENV=production
PORT=3001
```

---

## Task 8: VPS — Build backend + migrate + seed

- [ ] **Step 1: Installa dipendenze backend**

```bash
cd /opt/M.me/backend
npm ci
```
Expected: `added N packages` senza errori.

- [ ] **Step 2: Genera Prisma Client (prima di tsc)**

```bash
npx prisma generate
```
Expected: `Generated Prisma Client`. Il client deve esistere prima che tsc compili i file che lo importano.

- [ ] **Step 3: Compila TypeScript**

```bash
npm run build
```
Expected: nessun errore, directory `dist/` creata con `app.js`.

```bash
ls dist/app.js
```

- [ ] **Step 4: Esegui migrations**

```bash
npx prisma migrate deploy
```
Expected: `All migrations have been applied` o lista migration applicate.

- [ ] **Step 5: Seed database (SOLO PRIMA VOLTA)**

```bash
npx tsx prisma/seed.ts
```
Expected: output con Super Admin creato, ruoli e permessi di sistema inseriti.

---

## Task 9: VPS — Build frontend

- [ ] **Step 1: Installa dipendenze frontend**

```bash
cd /opt/M.me/frontend
npm ci
```

- [ ] **Step 2: Build produzione**

```bash
npm run build
```
Expected: `dist/` creata con `index.html` e assets.

```bash
ls dist/index.html
```

---

## Task 10: VPS — Nginx aggiornamento e SSL

- [ ] **Step 1: Sostituisci config Nginx**

```bash
cp /opt/M.me/docs/nginx/mirigliani.me /etc/nginx/sites-available/mirigliani.me
```

- [ ] **Step 2: Verifica config**

```bash
nginx -t
```
Expected: `syntax is ok` e `test is successful`.

- [ ] **Step 3: Reload Nginx (HTTP prima di SSL)**

```bash
systemctl reload nginx
```

- [ ] **Step 4: Ottieni certificato SSL**

```bash
certbot --nginx -d mirigliani.me -d www.mirigliani.me
```
Segui le istruzioni. Certbot aggiorna automaticamente la config Nginx con SSL.

- [ ] **Step 5: Verifica rinnovo automatico**

```bash
certbot renew --dry-run
```
Expected: `Congratulations, all simulated renewals succeeded`.

- [ ] **Step 6: Verifica Nginx finale**

```bash
nginx -t && systemctl reload nginx
```

---

## Task 11: VPS — PM2 start

- [ ] **Step 1: Avvia il processo**

```bash
cd /opt/M.me
pm2 start ecosystem.config.js
```
Expected: processo `mme` con status `online`.

- [ ] **Step 2: Verifica processo attivo**

```bash
pm2 list
```
Expected: riga `mme | online`. Processo `mps` ancora attivo — NON deve essere stato toccato.

- [ ] **Step 3: Salva lista processi PM2**

```bash
pm2 save
```
Expected: `PM2 | Saving current process list...`

- [ ] **Step 4: Verifica log (nessun errore di avvio)**

```bash
pm2 logs mme --lines 20 --nostream
```
Expected: `Backend in ascolto su http://0.0.0.0:3001`

---

## Task 12: Verifica end-to-end

- [ ] **Step 1: Test HTTPS homepage**

```bash
curl -s -o /dev/null -w "%{http_code}" https://mirigliani.me
```
Expected: `200`

- [ ] **Step 2: Test API health**

```bash
curl -s https://mirigliani.me/api/public/health | python3 -m json.tool
```
Expected: JSON con status OK (o 200).

- [ ] **Step 3: Test redirect www**

```bash
curl -s -o /dev/null -w "%{http_code} %{redirect_url}" http://www.mirigliani.me
```
Expected: `301 https://mirigliani.me/`

- [ ] **Step 4: Test redirect HTTP → HTTPS**

```bash
curl -s -o /dev/null -w "%{http_code} %{redirect_url}" http://mirigliani.me
```
Expected: `301 https://mirigliani.me/`

- [ ] **Step 5: Verifica MPS ancora attivo (isolamento)**

```bash
pm2 list | grep mps
curl -s -o /dev/null -w "%{http_code}" https://mirigliani.cloud/api/public/health
```
Expected: `mps | online` e `200`

- [ ] **Step 6: Commit finale spec aggiornata**

```bash
# Dal locale
cd /Users/spawn5m/Documents/DEV/M.me
git add docs/superpowers/specs/2026-04-16-deploy-design.md
git commit -m "docs(deploy): aggiorna spec — single domain, proxy /api/ e /uploads/"
git push origin main
```

---

## Checklist CLAUDE.md — aggiornamenti post-deploy

Dopo deploy completato, aggiornare `CLAUDE.md`:
- Fase 6 Deploy → `[x]`
- Branch attivo → `main` (deploy da main)
